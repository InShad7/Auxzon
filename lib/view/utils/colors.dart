import 'package:flutter/material.dart';

class AppColors {
  static const white = Colors.white;
  static const black = Colors.black;
  static const grey = Colors.grey;
  static const lightGrey = Color.fromARGB(113, 230, 230, 230);
  static const orange = Colors.orange;
  static const red = Color.fromRGBO(203, 32, 46, 1);
  static const lightRed = Color.fromRGBO(241, 164, 170, 0.594);
}
